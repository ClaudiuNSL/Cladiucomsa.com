"use client";
// TODO Task 11: fullscreen WebGL shader background — Simplex noise + chromatic aberration + grain
// Palette uniforms wired to useScrollPalette() baseRef/accentRef
export default function ShaderBackground() {
  return null;
}
